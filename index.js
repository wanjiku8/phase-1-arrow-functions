const add = (a, b) => a + b;

function multiplyNumbers(a, b) {
    return a * b;
  }

  const multiplyNumbers = (a, b) => a * b;


  const nums = [1, 2, 3];
  const squares = nums.map(num => num ** 2);
  console.log(squares); 
    